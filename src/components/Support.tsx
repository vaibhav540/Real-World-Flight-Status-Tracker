import React from "react";
import { Box } from "@chakra-ui/react";

const Support: React.FC = () => {
  return (
    <Box p={6}>
      For any issues, contact support at support@flightTracker.com.
    </Box>
  );
};

export default Support;

export {}; // Fix for --isolatedModules
